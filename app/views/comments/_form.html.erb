<%= form_for [@recipe, @comment] do |f| %>
  <%= f.label "Comment as #{current_user.username}:"%>
  <%= f.hidden_field :user, value: current_user.username %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit "Submit" %>
<% end %>
