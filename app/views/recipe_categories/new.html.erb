<h5>Add a Category to <%= @recipe.name %></h5>

<%= form_for [@recipe, @recipe_recipe_category] do |f| %>
   <%= f.select :category_id, options_for_select(Category.all.map {|c| [c.name, c.id]}) %>
   <%= f.submit "Update Recipe" %>
<% end %>
