<!DOCTYPE html>
<html>
  <head>
    <title>MenuManager</title>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css?family=Poppins:300,400,700" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <div class="flash">
      <% flash.each do |name, msg| -%>
        <%= content_tag :div, msg, class: name %>
      <% end %>
    </div>
    <header>
      <%= image_tag "dinnerset.svg", alt: "dinner set", class: "header-image" %>
       <h1>Menu Manager</h1>
    </header>
    <nav>
      <% if !current_user.nil? %>
      <div> Logged in as <%= current_user.username %></div>
      <div><%= link_to "Log Out", login_path, method: :delete %></div>
      <% else %>
      <div><%= link_to "Log In", login_path %></div>
      <% end %>
    </nav>
    <%= yield %>
  </body>
</html>
